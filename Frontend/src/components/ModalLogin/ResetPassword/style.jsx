import styled from 'styled-components';

const SResetPassword = styled.article`
  h2 {
    text-align: center;
  }
  button {
    width: 200px;
    background: var(--color-dark);
    color: white;
    border-radius: 30px;
    font-weight: bold;
    font-size: 1rem;
    padding: 0.5rem;
    margin: 20px calc((80vw - 200px) / 2);
  }
`;

export default SResetPassword;
