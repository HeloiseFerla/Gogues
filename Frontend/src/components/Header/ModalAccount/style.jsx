import styled from 'styled-components';

const SModalAccount = styled.ul`
  position: absolute;
  height: auto;
  background: white;
  right: 0px;
  top: 80px;
  z-index: 10;
  border-radius: 0px 0px 15px 15px;
  li {
    margin: 10px;
    button {
      font-weight: bold;
    }
  }
`;

export default SModalAccount;
